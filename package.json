{
  "name": "chrome-transcript",
  "version": "1.0.0",
  "description": "ChromeTranscript is a local Chrome extension that transcribes current-tab audio with Deepgram.",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "build": "npm run clean && node scripts/build.mjs",
    "dev": "npm run clean && node scripts/build.mjs --watch",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  },
  "engines": {
    "node": ">=20"
  }
}
