/** Shared default settings for transcript capture. */
import { DEFAULT_TRANSCRIPT_LANGUAGE } from './languages';
import type { TranscriptLanguage } from './types';

export const DEFAULT_LANGUAGE: TranscriptLanguage = DEFAULT_TRANSCRIPT_LANGUAGE;
